// Import and re-export the existing themes
import { lightTheme, darkTheme } from './theme/index';

// Export the existing themes
export { lightTheme, darkTheme };

// Export the light theme as default
export default lightTheme;